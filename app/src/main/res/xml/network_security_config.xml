<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security policy.

  Production: cleartext is denied (Android 9+ default), so all live traffic
  must go to https://api.neerly.in.

  Local dev: allow plain HTTP only to the loopback hosts the Android emulator
  uses to reach the host machine — `10.0.2.2` (emulator → host), `localhost`,
  and `127.0.0.1`. No way for a release build pointing at any real domain
  to fall back to cleartext.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
